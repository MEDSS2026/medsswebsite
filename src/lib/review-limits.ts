// Shared between the public review form and the /api/reviews endpoint.
// 280 chars ≈ 7 lines in the 340px testimonial card, so a max-length
// review always displays in full in the homepage marquee.
export const REVIEW_MAX_LENGTH = 280;
